---
description: 知识库「农场主」角色 — GraphRAG 结构维护、FillRequest、宪章同步
globs: INAGENT/agents/knowledge_farm_owner_agent.py,INAGENT/rag/graphrag_adapter.py,INAGENT/rag/graphrag_integration.py,INAGENT/rag/knowledge_schema.py,INAGENT/docs/agents/sessions/04-farm-owner.md,INAGENT/scripts/run_scenario_cultivation.py
alwaysApply: false
---

# 知识库会话：农场主（Farm Owner）

**权威宪章**：[`INAGENT/docs/agents/sessions/04-farm-owner.md`](INAGENT/docs/agents/sessions/04-farm-owner.md)  
**全会话锚点**（上下文压缩后仍注入）：[`kb-session-farm-owner-anchor.mdc`](kb-session-farm-owner-anchor.mdc)

## 持久职责（简记）

- 消费 **`schema_gaps.jsonl`**，gap 类型含 **`ambiguous_match`**；**TreeInformed**：只读 **CLIGraphStore / SkeletonIndex** → **`TreeContext`**（**`command_exists` → `(bool, List[str])`**；mock 须一致），批次内 **`_tree_context_cache`**；**批量 LLM 裁决**（conflict / overflow / ambiguous / new_entity 未定项），**`needs_tree_session` → `FarmOwnerReport.deferred`**；可选 **`classify_uncovered_chunks`**（`chunk_meta_patch`、`target_block_id`）；**`cultivate_scenarios`** 写 **`reference/scenarios_scaffold.json`**（农民写 **`scenarios_synthesized.json`**）。写入前 **`snapshot_backup`**；**`reload()`**。混合向量默认不刷；**`refresh_hybrid_vectors=True`**：`reload` → **`merge_knowledge_base`** → **`refresh_hybrid_vector_index`**（默认 **`hybrid_vectors_force=True`** 全量 Qdrant；指纹模式见宪章）。**Qdrant 无按条增量 upsert**。**E2E** 顺序见宪章 § 混合向量刷新与农民。
- **结构边界**：**新建** 叶/枝/干/根、**挖槽/新列/契约级 metadata 形态** 均属农场主；农民只在已有节点上补内容与执行 `FillRequest`（见 `04-farm-owner.md` § 与农民的结构边界）。
- **`FarmOwnerReport`**：`fill_requests`、`deferred`、`operation_log` / `tree_mutations`、`discarded_*`、`errors`。**`new_entity_attribute`** 仅 **`default_value is not None`** 时发 `FillRequest`。**不**做 chunk 内容富化。编排须调用农民 **`apply_fill_request`** 将 `fill_requests` **批量**落盘到 `reference/*.json`（及可选骨架）；农场主模块**不**内联执行回填。
- 与 **树** 会话：CLI 与图不一致时 **先定数据归属** 再改代码。
- **职责变更时**：同步 **`04-farm-owner.md`**、本文件、锚点、**`knowledge_farm_owner_agent.py` 模块头**；契约变更同步农民/采购单测与 PR。
- **闭环/合并（骨架已有，非农场主 Agent 内建）**：`quality_feedback_loop` + purge/manifest 脚本、父子 **`merge_reference_parent_child`** 见 **`DATA_FLOW` §8** 与 **`PLAN_CLOSED_LOOP_QA_AND_PARENT_DOC_MERGE.md`**；自动写 `_owner_quality_rules.json`、GraphRAG 批量 purge、CLI 自动推断父子 **仍非**现成能力。**不写**完整 `knowledge_base.json`（合并属 `merge_knowledge_base`/编排）。

## 范围（应改）

- `INAGENT/agents/knowledge_farm_owner_agent.py`
- `INAGENT/rag/graphrag_adapter.py`、`INAGENT/rag/graphrag_integration.py` 中与 farm_owner **结构写入**直接相关、且已确认安全的调用点（小步、可回滚）
- `INAGENT/rag/knowledge_schema.py`（FillRequest / SchemaGapEntry / FarmOwnerReport / Tree*）
- `INAGENT/scripts/run_scenario_cultivation.py`；`farm_owner_overflow_denylist.json`；若动混合向量收尾路径则协调 **`workflow_config_generator.refresh_hybrid_vector_index`**、`merge_knowledge_base` 与 **`04` / `06-hybrid-search`**

## 非范围（勿改）

- chunk **内容富化**（农民 `apply_fill_request` 与 reference）
- **采购** 三层准入策略
- **销售员** 检索路由与 Rerank（除非修复农场主 **reload** 后的集成错误）
