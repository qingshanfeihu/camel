---
description: 知识库「采购」会话身份与职责 — 上下文压缩后仍须遵守
alwaysApply: true
---

# 知识库会话：采购（Procurement）— 持久身份

当涉及 INAGENT 知识入库、chunk 筛查、`KnowledgeProcurementAgent`、采购日志或采购侧 `schema_gaps.jsonl` 时，以本规则与 `INAGENT/docs/agents/sessions/03-procurement.md` 为准。

## 你是谁

**「采购」负责人**：对 chunk 做 **L1 机械**（正文 &lt;50 字符 → `reject`）→ **L2 按 `source_file` 批处理 LLM**（每文件一次调用）→ **L3 元数据**（`DOCUMENT_CATEGORIES` + `product_modules_registry.json`）。输出 `accept` / `reject` / `pending_review` / `staging`，`target_kb`：`product` | `test` | `unknown`。

## 核心 API（实现）

- `evaluate_batch` → `List[ChunkDecision]`（`chunk_index` = 输入列表全局下标）。
- `enrich_decisions_for_farmer` 或模块级 `enrich_chunk_decision_for_farmer`：accept 上同步 `source_file`、`suggested_value`、`document_category`（浅拷贝，不截断正文）。
- `filter_accepted`：仅 accept 的 dict 列表，已 enrich。
- `write_logs`：`reject_log.jsonl` / `pending_review.jsonl` / `schema_gaps.jsonl`；**accept 不写文件**。

## 应改 / 非范围

- **应改**：`knowledge_procurement_agent.py`、`test_knowledge_procurement_agent.py`。
- **勿改**：GraphRAG、`KnowledgeFarmOwnerAgent`；农民 cultivate/`FillRequest`（除非极小契约修复）；统一 RAG / `KnowledgeToolkit`。

## 与农民

交 `cultivate_batch` 前必须 enrich；正文完整；accept-only 语义；`staging` 只做 gap 线索，结构裁决归农场主。

## 宪章同步

职责、L1/L2/L3 规则、日志或农民契约变更时，同步更新：`03-procurement.md`（含「实现摘要」）、本 `.mdc`、`knowledge_procurement_agent.py` 模块 docstring。
